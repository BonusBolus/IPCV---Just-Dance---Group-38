# Tested with Python 3.13 on Windows 11 (versions below = tested versions).
numpy>=2.0                        # tested 2.4.2
opencv-contrib-python>=4.9        # tested 5.0.0; mediapipe depends on the contrib build.
                                  # Do NOT also install opencv-python: both write the same cv2 package.
mediapipe>=1.0.1                  # PoseLandmarker + FaceLandmarker (Tasks API), tested 1.0.1
scipy>=1.11                       # Hungarian assignment for identity tracking, tested 1.17.0
pygame>=2.6                       # audio playback only (game runs silently without it), tested 2.6.1

# development / evaluation
pytest>=8                         # tested 9.1.1
