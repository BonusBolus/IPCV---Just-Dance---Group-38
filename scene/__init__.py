"""Task 5: Scene processing and real-time integration (team lead).  Owner: <name> (TBD)

state_machine.py  START -> LOBBY -> COUNTDOWN -> PLAYING -> RESULTS
audio.py          song playback; its position is the game's master clock
background.py     stage background, background replacement, scene reactions
renderer.py       composes every layer into the final frame
"""
