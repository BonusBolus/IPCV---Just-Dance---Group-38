"""Task 1: Face tracking and augmented effects.  Owner: <name> (TBD)

face_tracker.py  detect faces + head orientation per frame  -> list[FaceObs]
face_filter.py   per-player temporal smoothing              -> FaceObs
face_effects.py  aligned face effect + player tag/HUD above the head
"""
