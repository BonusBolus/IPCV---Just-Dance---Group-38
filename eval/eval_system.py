"""Task 5 evaluation. TODO(T5).

Suggested metrics:
  - end-to-end FPS and per-stage times: run `python main.py --profile-csv logs/run.csv`
    and summarise the CSV (columns: frame, section, ms) per section (mean, p95)
  - glass-to-glass latency: film a stopwatch/LED and the screen with a phone
  - audio/choreography sync drift over a full song
  - behaviour under difficult conditions: low light, one player leaves, module failure
"""


def main() -> None:
    raise SystemExit("Task 5 evaluation not implemented yet")


if __name__ == "__main__":
    main()
