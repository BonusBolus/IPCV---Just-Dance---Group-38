"""Evaluation scripts, one per task. Each should produce the numbers/plots for its report section.

Run on recorded sessions (tools/record_session.py) so results are reproducible.
Run from the repo root with `python -m eval.<name> --video recordings/<file>.mp4`.
"""
